<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="30">
    <Properties>
        <!-- Define log file paths -->
        <Property name="LOG_PATTERN">%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n</Property>
        <Property name="LOG_FILE_PATH">logs</Property>
        <Property name="ARCHIVE_FILE_PATH">logs/archive</Property>
    </Properties>

    <Appenders>
        <!-- Console Appender -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${LOG_PATTERN}"/>
        </Console>

        <!-- File Appender for all logs -->
        <RollingFile name="FileAppender" fileName="${LOG_FILE_PATH}/application.log"
                     filePattern="${ARCHIVE_FILE_PATH}/application-%d{yyyy-MM-dd}-%i.log.gz">
            <PatternLayout pattern="${LOG_PATTERN}"/>
            <Policies>
                <TimeBasedTriggeringPolicy/>
                <SizeBasedTriggeringPolicy size="10MB"/>
            </Policies>
            <DefaultRolloverStrategy max="10"/>
        </RollingFile>

        <!-- Error File Appender -->
        <RollingFile name="ErrorFileAppender" fileName="${LOG_FILE_PATH}/error.log"
                     filePattern="${ARCHIVE_FILE_PATH}/error-%d{yyyy-MM-dd}-%i.log.gz">
            <PatternLayout pattern="${LOG_PATTERN}"/>
            <Policies>
                <TimeBasedTriggeringPolicy/>
                <SizeBasedTriggeringPolicy size="10MB"/>
            </Policies>
            <DefaultRolloverStrategy max="10"/>
        </RollingFile>

        <!-- Performance Log Appender -->
        <RollingFile name="PerformanceAppender" fileName="${LOG_FILE_PATH}/performance.log"
                     filePattern="${ARCHIVE_FILE_PATH}/performance-%d{yyyy-MM-dd}-%i.log.gz">
            <PatternLayout pattern="${LOG_PATTERN}"/>
            <Policies>
                <TimeBasedTriggeringPolicy/>
                <SizeBasedTriggeringPolicy size="10MB"/>
            </Policies>
            <DefaultRolloverStrategy max="10"/>
        </RollingFile>

        <!-- Async Appenders for better performance -->
        <Async name="AsyncConsole">
            <AppenderRef ref="Console"/>
        </Async>

        <Async name="AsyncFile">
            <AppenderRef ref="FileAppender"/>
        </Async>

        <Async name="AsyncError">
            <AppenderRef ref="ErrorFileAppender"/>
        </Async>

        <Async name="AsyncPerformance">
            <AppenderRef ref="PerformanceAppender"/>
        </Async>
    </Appenders>

    <Loggers>
        <!-- Root Logger -->
        <Root level="info">
            <AppenderRef ref="AsyncConsole"/>
            <AppenderRef ref="AsyncFile"/>
        </Root>

        <!-- Application specific loggers -->
        <Logger name="com.ecommerce" level="debug" additivity="false">
            <AppenderRef ref="AsyncConsole"/>
            <AppenderRef ref="AsyncFile"/>
        </Logger>

        <!-- Error Logger -->
        <Logger name="ErrorLogger" level="error" additivity="false">
            <AppenderRef ref="AsyncConsole"/>
            <AppenderRef ref="AsyncError"/>
        </Logger>

        <!-- Performance Logger -->
        <Logger name="PerformanceLogger" level="info" additivity="false">
            <AppenderRef ref="AsyncPerformance"/>
        </Logger>

        <!-- Spring Framework Loggers -->
        <Logger name="org.springframework" level="info" additivity="false">
            <AppenderRef ref="AsyncConsole"/>
            <AppenderRef ref="AsyncFile"/>
        </Logger>

        <!-- Hibernate/SQL Loggers -->
        <Logger name="org.hibernate.SQL" level="debug" additivity="false">
            <AppenderRef ref="AsyncConsole"/>
            <AppenderRef ref="AsyncFile"/>
        </Logger>

        <Logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="trace" additivity="false">
            <AppenderRef ref="AsyncConsole"/>
            <AppenderRef ref="AsyncFile"/>
        </Logger>

        <!-- Security Loggers -->
        <Logger name="org.springframework.security" level="debug" additivity="false">
            <AppenderRef ref="AsyncConsole"/>
            <AppenderRef ref="AsyncFile"/>
        </Logger>

        <!-- Eureka/Discovery Loggers -->
        <Logger name="com.netflix.discovery" level="info" additivity="false">
            <AppenderRef ref="AsyncConsole"/>
            <AppenderRef ref="AsyncFile"/>
        </Logger>

        <!-- Gateway Loggers -->
        <Logger name="org.springframework.cloud.gateway" level="debug" additivity="false">
            <AppenderRef ref="AsyncConsole"/>
            <AppenderRef ref="AsyncFile"/>
        </Logger>

        <!-- Reduce noise from some verbose loggers -->
        <Logger name="org.apache.catalina.startup.DigesterFactory" level="error" additivity="false">
            <AppenderRef ref="AsyncConsole"/>
        </Logger>

        <Logger name="org.apache.catalina.util.LifecycleBase" level="error" additivity="false">
            <AppenderRef ref="AsyncConsole"/>
        </Logger>

        <Logger name="org.apache.coyote.http11.Http11NioProtocol" level="warn" additivity="false">
            <AppenderRef ref="AsyncConsole"/>
        </Logger>

        <Logger name="org.apache.sshd.common.util.SecurityUtils" level="warn" additivity="false">
            <AppenderRef ref="AsyncConsole"/>
        </Logger>

        <Logger name="org.apache.tomcat.util.net.NioSelectorPool" level="warn" additivity="false">
            <AppenderRef ref="AsyncConsole"/>
        </Logger>

        <Logger name="org.eclipse.jetty.util.component.AbstractLifeCycle" level="error" additivity="false">
            <AppenderRef ref="AsyncConsole"/>
        </Logger>

        <Logger name="org.hibernate.validator.internal.util.Version" level="warn" additivity="false">
            <AppenderRef ref="AsyncConsole"/>
        </Logger>
    </Loggers>
</Configuration> 